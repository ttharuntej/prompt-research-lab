# Required Settings
OPENAI_API_KEY=your-openai-api-key-here
GROQ_API_KEY=your-groq-api-key-here

# Note: All other settings (models, tokens, batch size, etc.) 
# are configured in src/config.py with sensible defaults.
# You can override any setting here if needed for your environment. 